{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <br>
    <a href="{{ path('app_admin_category_index') }}">Retout a la liste</a>
    <br>
    <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}">Modifier</a>
    <br>
    {{ include('admin_category/_delete_form.html.twig') }}
    <br>
{% endblock %}
