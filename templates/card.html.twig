  
    
      <div class="card m-2">
        <img src="/../../uploads/images/{{ article.path }}" class="card-img-top article-img" alt="images article"> 
        <div class="card-body mt-2">
          <h5 class="card-title"> {{ article.title }} </h5>
          <p class="card-text"> {{ article.content|truncate(100) }} </p>
          <a  class="btn btn-primary" href="{{ path('app_article_show', {'id': article.id}) }}">Consulter</a>
        </div>
      </div>
    
  



